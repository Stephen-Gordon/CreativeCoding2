/* function drawGrid(numBoxes, color,weight) {
    let boxSize= width/numBoxes;
    for (let j = 0; j < numBoxes; j++) {

        for (let i = 0; i < numBoxes; i++) {
            strokeWeight(weight);
            stroke(color);
            rect(boxSize * i, boxSize * j, boxSize ,boxSize)
        }


    }
    return "drawn";

} */